version: '3.9'
services:
    api:
        build:
            dockerfile: Dockerfile
            context: .
            target: prod
        volumes:
            - .:/app
        command: yarn run start
        ports:
            - 3000:3000
            - 9229:9229
        restart: always
