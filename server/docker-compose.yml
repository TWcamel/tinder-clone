version: '3.9'
services:
    api:
        build:
            dockerfile: Dockerfile
            context: .
            target: development
        volumes:
            - .:/app
        command: yarn run debug
        environment:
            NODE_ENV: development
            PORT: 3000
        ports:
            - 8080:3000
            - 9229:9229
        restart: always
